Bangun seluruh route Express/Hono:

1. Account Requests

    * POST /account-requests

    * GET /account-requests

    * GET /account-requests/:id

2. Approval

    * GET /approvals/mine

    * POST /approvals/:id/approve

    * POST /approvals/:id/reject

3. Documents

    * POST /documents/upload

    * GET /documents/user/:id

4. Notification

    * GET /notifications

    * POST /notifications/mark-read

Tambahkan middleware:
authenticate, authorize, approvalGuard.(buat yang belum ada)

Tulis seluruh implementasi route lengkap.