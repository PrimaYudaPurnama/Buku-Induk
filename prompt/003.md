Buatkan approval engine yang mencakup:

* Fungsi generateApprovalSteps(request_type, user_data, payload)

* Fungsi createApprovalsForRequest(account_request)

* Fungsi approveStep(approval_id, approver_user)

* Fungsi rejectStep(approval_id, approver_user)

* Fungsi checkIfAllApproved(request_id)

* Fungsi finalizeRequest(request) (buat history, document jika ada, kirim notification)

Aturan:

* Jika satu step ditolak → semua approval auto rejected, request juga rejected.

* Jika satu step disetujui → hanya step berikutnya membuka status pending.

* Tidak boleh approve level >1 sebelum level sebelumnya approved.

Buat kode clean + modular